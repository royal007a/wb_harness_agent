# Evidence

按任务保存结构化证据：`<task_id>/<timestamp>-<kind>.*`。

证据至少记录：任务与运行标识、代码/配置/依赖版本、环境、命令或复核步骤、预期、实际结果、生成时间和负责人。

不得包含密钥、完整敏感正文、思维过程或未脱敏个人数据。大文件保存到受控对象存储时，此处只写内容摘要、访问范围和不可变引用。

任务标记完成前，`manifest.json` 必须列出适用的质量门禁及其 Evidence 路径。
