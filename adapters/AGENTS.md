# 引擎适配器边界

- 不导入 backend.store 或写 Run 终态；通过受校验事件和 AdapterResult 返回。
- 任务、运行、资源均为输入快照；不接收数据库对象、凭证正文或任意宿主目录。
- 取消、超时、预算和清理必须可观察。
- CodeAgent 必须显式传入隔离 executor，不依赖默认本地执行器。
- 脚本模型只用于测试；不得登记为生产模型或启用真实引擎路由。
