# HA-0008 真实模型接入探针

状态：blocked，尚未开始模型调用。

阻塞：当前项目进程未配置批准的模型端点、模型 ID、凭证引用和费用上限。没有搜索或复用其他项目密钥。

恢复条件：用户指定可用于本项目的端点、模型 ID、本地凭证配置位置/环境变量引用、测试预算及可发送的数据范围。不要在群聊发送密钥正文。

后续范围：验证连接、真实工具/代码响应、Token/费用、超时取消；实现面向单列分组聚合的真实适配器与独立结果验收，再开放可选路由。不得静默切换模型或把脚本模型当作接入成功。生产隔离、模型恢复和 Claude P1 另行验收。
