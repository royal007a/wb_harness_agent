# HA-0006 本地前后端纵向切片

用户授权：2026-09-12 群内“初版 harness 代码，前后端”及“实现”。

步骤：确定 ADR-0009 → 实现持久控制面和固定分析 Adapter → 实现工作台 → 契约、失败/取消/重启和浏览器验证 → 文档与证据。

验收：上传 CSV 后生成可下载报告、图表、manifest；记录不可变 Task 和多个 Run；幂等、防越权配置、取消、超时和重启有验证证据；页面可创建、选择、重跑、取消和查事件；未知引擎明确不可用。

边界：本地单用户原型。无真实模型、任意代码执行、远程部署和 P2 记忆。此前 HA-0002 的完整模拟契约（含 checkpoint 等）不因该切片自动验收。

回滚：回退本次实现提交；先停止本地服务，保留 `.local/` 数据库备份。不得以回滚删除用户 CSV。

完成：2026-09-12。本地切片已部署到 `127.0.0.1:8765`，31 项后端测试与 11 个浏览器检查通过；桌面/移动截图已查看。证据见 `harness/evidence/HA-0006/manifest.json`。该结论不替代生产 L3 或完整 CodeAct 验收。
