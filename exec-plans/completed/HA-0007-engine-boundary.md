# HA-0007 引擎契约和隔离执行准备

授权：用户引用下一步路线并要求“继续”。

已完成范围：统一固定分析器 adapter lifecycle 与事件/结果边界；固定分析器回归；Colima 隔离容器探针；真实 SDK + 脚本模型多轮执行验证；项目内确定性分组聚合 Skill/CLI；工作台显示 readiness；本机服务更新。没有交付真实模型目标驱动的分组报告链路，该部分保持待接入。

验收：现有工作台无回归；未知/伪造引擎事件不能提交成功；重复启动、取消、超时、清理错误明确；沙箱只读输入、禁网、非 root、限制资源与退出销毁探针通过；真实 Smolagents SDK 在脚本模型下完成多轮容器执行；缺失模型配置时界面显示具体待接入项。

真实模型效果验收需已批准模型端点、模型 ID、凭证引用和预算配置。未取得时记录未完成事实，完成其余独立工作，不把脚本模型冒充模型接入。

部署：保持 8765，本地更新前做临时库/浏览器验证，重启后检查历史任务。回滚新提交并重启，保留现有 SQLite。

验收结果（2026-09-12）：59 项全量测试通过，其中 23 项 SDK/VM/CLI 专项；12 项浏览器检查通过，无 console/page error，390px 无横向溢出。Skill 结构验证通过。launchd 重启后健康接口正常，4 条历史/验收任务可查询；本项目探针容器无残留。证据见 `harness/evidence/HA-0007/`。

课程补充已落入 SKILL_EXECUTION.md；采用 skill-creator 的短入口 + 固定脚本结构，不引入全局 Skill 安装或隐式 SDK 路由。真实模型连接仍需用户指定，见 HA-0008 阻塞计划。
