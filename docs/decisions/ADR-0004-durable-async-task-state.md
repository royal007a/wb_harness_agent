# ADR-0004：采用持久异步任务状态机

- 状态：Proposed
- 日期：2026-09-08

## 背景

Agent 任务可能持续较久、等待人工批准、调用不稳定依赖或跨服务执行。仅用同步 HTTP 请求和内存状态无法可靠取消、恢复与审计。

## 决策

建议以持久任务、运行、检查点、租约和只追加事件为核心；使用 Outbox 保持状态与事件发布一致。同步请求仅创建或查询任务。

## 后果

- 可支持服务重启、重试、取消、超时和人工批准。
- 系统复杂度增加，需要解决重复投递、租约竞争、事件顺序和终态一致性。
- 具体数据库与队列技术仍待原型验证后决定。
