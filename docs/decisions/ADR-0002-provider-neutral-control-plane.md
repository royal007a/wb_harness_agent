# ADR-0002：采用厂商中立控制面契约

- 状态：Proposed
- 日期：2026-09-08

## 背景

不同 Agent 引擎的消息、工具、状态和错误模型差异明显。若核心领域直接使用某一 SDK 类型，替换成本和治理盲区会扩散到整个系统。

## 决策

核心只定义任务、运行、事件、工具、批准、产物和评测等中立契约。特定引擎能力通过能力声明与命名空间扩展表达。

## 后果

- 控制面、权限、可观测和评测可以独立演进。
- 不能假设所有引擎支持完全相同的功能。
- 公共契约必须保持最小且严格，适配器需承担语义映射和能力缺失报告。
