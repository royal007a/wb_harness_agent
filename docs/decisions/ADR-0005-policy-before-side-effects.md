# ADR-0005：副作用前执行策略与授权

- 状态：Proposed
- 日期：2026-09-08

## 背景

模型输出和外部内容均不可信。若工具自行执行后再审计，权限拒绝已经失去意义，高风险动作也无法被可靠拦截。

## 决策

所有工具请求先经过 Schema 校验、策略判断和风险分类，再进入隔离执行。需要人工批准的动作暂停任务；批准令牌绑定具体动作摘要并具有次数和时间限制。

## 后果

- 未授权副作用可以在执行前被阻断。
- 工具目录必须准确声明风险、网络、文件和幂等属性。
- 参数变化、工具版本变化或批准过期后必须重新判断和批准。
