# ADR-0003：引擎以可选适配器接入

- 状态：Proposed
- 日期：2026-09-08

## 背景

候选框架覆盖的数据分析、长文生成、复杂编排、知识工程和文档审查能力不同，同时也有各自的语言、部署和安全约束。

## 决策

每个执行引擎通过独立适配器接入，按真实场景逐个评估和实施。首个纵向切片使用模拟适配器；任何真实适配器都不是基础安装的强制依赖。

## 后果

- 可以按价值渐进接入，避免一次集成全部框架。
- 需要维护能力矩阵、适配器契约测试和版本兼容策略。
- 运行中的引擎选择必须显式并记录，不允许静默跨引擎切换。
